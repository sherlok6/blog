<h1>Все посты</h1>
<% @posts.each do |post|%>
    <div class="container">
        <p><%= post.title %></p>
        <p><%= post.dt_post %></p>
        <%= link_to "Прочитать пост", post_path(post) %>
        <br>
        <strong>Автор: <%= post.user.email %></strong>
    </div>
    <br>
<% end %>